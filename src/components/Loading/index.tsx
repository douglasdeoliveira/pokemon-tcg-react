import React from 'react';

import { Container } from './styles';

const Loading: React.FC = () => {
  return (
    <Container>
      <span>Loading...</span>
    </Container>
  );
};

export default Loading;
